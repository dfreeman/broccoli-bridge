import { Changes } from "./interfaces";
export default class TreeDiff {
    private path;
    changes: Changes;
    private last;
    private current;
    constructor(path: string);
    recompute(): void;
}
